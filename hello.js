let init = () => {
    document.addEventListener('DOMContentLoaded', async () => {
        let hash_confirm_button = document.querySelector("#hash_confirm")
        hash_confirm_button.addEventListener("click", () => {
            console.log("ggggg");
        })
    })
}

init();